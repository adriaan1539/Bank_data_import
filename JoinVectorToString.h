#ifndef JOINVECTORTOSTRING_H
#define JOINVECTORTOSTRING_H

#include <string>
#include <vector>

std::string JoinVector(std::vector<std::string>, std::string delimiter);
std::string JoinVector(std::vector<int>, std::string delimiter);

#endif
